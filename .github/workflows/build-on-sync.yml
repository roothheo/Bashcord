name: 🔨 Build after Sync

on:
  # Se déclenche après une synchronisation réussie
  workflow_run:
    workflows: ["🔄 Sync with Equicord"]
    types:
      - completed

jobs:
  build:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 📦 Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: 📚 Install dependencies
        run: pnpm install --frozen-lockfile

      - name: 🔨 Build
        run: pnpm build

      - name: ✅ Build successful
        run: |
          echo "### ✅ Build réussi" >> $GITHUB_STEP_SUMMARY
          echo "Bashcord est prêt après la synchronisation avec Equicord" >> $GITHUB_STEP_SUMMARY

      - name: 📦 Upload artifacts (optional)
        uses: actions/upload-artifact@v4
        with:
          name: bashcord-build
          path: dist/
          retention-days: 7

